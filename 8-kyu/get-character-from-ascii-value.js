/* 
    Write a function get_char() / getChar() which takes
    a number and returns the corresponding ASCII char
    for that value
*/

const getChar = (c) => {
    return String.fromCharCode(c);
};

/* 
    Implicit return version:
    
    const getChar = c => String.fromCharCode(c);
*/
